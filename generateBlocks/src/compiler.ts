import * as Preprocessed from "./PreprocessedSchema.js";

type compilationOutput = {
    blocksCode: string
    toolboxCode: string
}
export function compile(preprocessed: Preprocessed.Schema): any /* compilationOutput */ {
    
}
